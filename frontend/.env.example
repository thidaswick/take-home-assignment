VITE_API_URL=http://localhost:5108/api

# Production (Vercel) example:
# VITE_API_URL=https://YOUR-API.up.railway.app/api

# Optional: set VITE_USE_MOCK=true to use in-memory mock data instead of the API
